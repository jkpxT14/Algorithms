#include <iostream>
#include <string>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <algorithm>
#include <utility>
#include <tuple>
// #include <limits>

using namespace std;

using ll=long long;
using vi=vector<int>;
using vb=vector<bool>;
using si=stack<int>;
using qi=queue<int>;
using dqi=deque<int>;
using pii=pair<int, int>;
using vpii=vector<pair<int, int>>;
using vipii=vector<int, pair<int, int>>;
using dqpii=deque<pair<int, int>>;
using vvpii=vector<vector<pair<int, int>>>;

#define INT_MAX 2147483647
const int INF=INT_MAX;

// ans = answer
// cur = current

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    // cout.tie(nullptr);

    int N=2e4;
    cout<<N;

    return 0;
}